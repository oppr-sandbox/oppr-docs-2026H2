# Analyses index

Append new entries at the top. Keep one line per analysis.

| Slug | Date | Scope | Status | Title |
|---|---|---|---|---|
| `document-versioning` | 2026-05-22 | desktop + data-model | Shipped (Option A) | Forked-edition versioning + library lifecycle. Published SOP stays live while the next edition is forked/authored/reviewed/approved; replaces the live version only on a confirmed re-publish (→ v2). Root cause: single status row — saveContent flips published→draft on first edit, taking v1 offline with no replace gate. Decision 1 (gates): Option A — add liveVersion beside currentVersion on one row (vs Option B forked row). Serving: operators/QR/RAG read liveVersion, editor works on currentVersion. Adds replace-confirmation AlertDialog + library 'v2 in progress' chip/expansion. Toast + breadcrumb fixes shipped separately. 4 decisions, 1 gates. |
| `diagram-builder-rebuild` | 2026-05-22 | desktop + Editor | Built (awaiting use) | Ground-up rebuild spec. Root cause: css-size+overflow canvas is wrong. Decision 1: fixed SVG viewport + single <g transform> (pan by drag, zoom in+out freely, add-shape at viewport centre). Decision 2: background as placeable/lockable element, not canvas-size driver. Consolidate 'Start from' → 'Import template' dropdown (+ Background entry); drag-and-drop palette. Scope guard: no new capabilities. Interdependency map keeps DiagramModel/data-svg/TipTap contract stable. 4 open decisions, 1&2 gate. |
| `diagram-builder` | 2026-05-22 | desktop + Editor + AI | Superseded (v1–v3) | Diagram builder spec behind the picker placeholder. v1 built: diagramModel.ts + renderDiagramSvg.ts + DiagramBuilder.tsx canvas + DiagramBlock data-model round-trip + Builder/Presets tabs + hover-Edit. Lead decision: model-first (typed DiagramModel as source of truth, pure renderDiagramSvg cached into data-svg so read view + PDF unchanged) vs SVG-only — recommend model-first. Build: hand-rolled SVG canvas (React Flow rejected for DOM/export mismatch, mermaid as escape hatch, AI-raw-SVG rejected for safety). Synthesis: AI drafts the model, human refines. v1 cut: straight/elbow connectors, no obstacle avoidance. Closes the dangerouslySetInnerHTML hole. 5 open decisions, 1–2 gate everything. |
| `import-redesign` | 2026-05-22 | desktop + data-model | Shipped | Import pipeline redesign (v2, slimmed after scan-PDF grilling): two outcomes by file type — text PDFs extract straight to the editor as pre_draft (headings/bold/italic/tables only, images committed on save); scans route to attach-as-PDF. Deletes AI-map, cross-links, and the naming-code gate. Root cause: scannedPdf misclassification + full-page raster fallback + AI-on-empty-text + finalize naming gate. 4 decisions settled. |
| `pdf-and-authoring-polish` | 2026-05-21 | desktop + data-model | Shipped | PDF export + authoring polish: machine chip/icon (vs #), missing export sections (embedded PDF, asset list, references — root cause unsaved-state + no pdfAttachment renderer), grouped always-on publish dialog, condensed metadata panel with machine names + reference docs, insert-image library tab |
| `authoring-completeness` | 2026-05-21 | desktop + data-model | Shipped | Authoring completeness & lifecycle: fresh-start to one reference doc, gated author/reviewer/approver cycle (+approved state, role fields, sign-off trail), DB-backed /templates manager, configurable per-triplet naming counter + /settings/naming vocabulary, metadata cleanup (drop tags + manual assets, add location/discipline + body-derived links), asset pill code-vs-code+name, corner-drag image resize |
| `claude-stack-template` | 2026-05-08 | desktop + AI + data-model | Shipped | Claude stack template: reusable starter at ../claude-stack-template/ with Vite/React/TS/Tailwind/shadcn/Convex/Convex Auth magic link/wouter + layout system + analysis-page skill + Dashboard/Example/Settings/Analysis pages — 69 files, tsc 0, vite build 0 |
| `mobile-revamp` | 2026-05-07 | mobile + AI + data-model | Shipped | Mobile interface revamp: hard crash on stale localStorage doc ids post-wipe (looksLikeConvexId fence + resolveMany recovery), density pass for 430-wide phone, PPE Popover with label + description, IDA A−/A+ font-size control persisted, autosizing composer textarea capped at 6 lines, search-first home + duplicate disambiguation |
| `eddycurrent-importer-walkthrough-v2` | 2026-05-07 | desktop + data-model | Shipped | Eddycurrent SOP run #2 after StructuredDoc shipped: COEP dropped, cross-link \"Will skip\" + empty-library banner, doc-delete sweeps imageUsages, image library multi-select + bulk-delete + group-by-document view |
| `eddycurrent-importer-walkthrough` | 2026-05-07 | importer + AI + data-model + renderer | In progress | Real customer SOP through /import: structure loss audit + StructuredDoc JSON intermediate proposal |
| `editor-overhaul` | 2026-05-07 | desktop + data-model | Done | Editor overhaul: image preview, toolbar grouping, table controls, document-creation flow, sticky+consistent chrome, print fidelity (P1+P2+P3 chooser+P5 chrome shipped; P3 state model and P4 construction extras follow up separately) |
| `image-library` | 2026-05-07 | desktop + data-model | Done | Image library: upload flow + repository surface, replaces window.prompt URL flow |
| `page-header-spec` | 2026-05-07 | desktop + AI | Outstanding | Page header spec: TopBar / PageHeader / floating Ask IDA two-tier system |
| `gap-analysis` | 2026-05-06 | desktop + mobile + AI + data-model | Open (snapshot) | Gap analysis: showcase vs v1.0 spec vs full-fat DOCS |
| `tiptap-editor-revamp` | 2026-05-06 | desktop + AI + data-model | Shipped | Editor reorder, safety palette, asset/log linking upgrades |
| `ida-sources-and-clear-modal` | 2026-05-06 | mobile + AI | Shipped | Clear-chat modal styling + IDA citing non-connected docs + ungrouped sources |
